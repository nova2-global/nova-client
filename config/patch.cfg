# Patch server overrides. Both keys are optional; the exe carries compiled-in
# defaults. Command line --patch-url / --patch-channel win over this file.
#PATCH_URL http://127.0.0.1:8080
#PATCH_CHANNEL live
